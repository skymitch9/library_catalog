{
  "name": "@lc/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: HTTP routing and auth. All logic lives in packages/.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:friend": "wrangler deploy --env friend",
    "db:migrate:local": "wrangler d1 migrations apply library-catalog --local",
    "db:migrate": "wrangler d1 migrations apply library-catalog --remote",
    "db:migrate:friend": "wrangler d1 migrations apply library-catalog-2nd --remote --env friend",
    "tail": "wrangler tail",
    "tail:friend": "wrangler tail --env friend",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@lc/core": "*",
    "@lc/db": "*",
    "@lc/estate-auth": "*",
    "@lc/gabi-conv": "*",
    "@lc/isbn": "*",
    "@lc/research": "*",
    "@lc/universes": "*",
    "hono": "^4.6.14",
    "jose": "^5.9.6",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0"
  }
}
