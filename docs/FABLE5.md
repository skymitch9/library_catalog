# Fable 5 — briefing and work queue

> **Audience:** Claude sessions dispatching Fable 5, and Fable 5 itself.
> **Status:** TRACKED. Created **2026-08-13**. Last verified: **2026-08-13**.
>
> Dispatch with the `Agent` tool and `model: 'fable'`. In a `Workflow`, use
> `agent(prompt, { model: 'fable' })`.

## 1. Why this document exists

The owner, 2026-08-13: *"we haven't used any fable 5 usage this month, we should
plan to use that for something. its waste to leave our powerhouse on the table
unused."*

⚠️ **Fable has its own weekly allowance, counted separately from the all-models
pool.** Measured on the usage page that day: **Fable weekly 0%** beside
**all-models weekly 52%**. So Fable work does not spend the budget the rest of the
session draws on. Leaving it at zero is waste; spending it on the wrong thing is
also waste.

## 2. What Fable is for here: the review gate

**Chosen split, 2026-08-13:**

| Fable 5 | Opus |
|---|---|
| Adversarially review **every change before it deploys** | Build features, run the data work |
| **Design** the edit-any-detail + audit-log feature | Apply Fable's design *after the owner approves it* |
| Review **any migration before it runs** | Everything in `docs/TODO.md` → "Agent-safe" |

### ⚠️ Why a review gate rather than a second builder

This is the argument for the split, and it is empirical rather than theoretical.
In one **attended** evening the Opus session got three things wrong and caught
each only afterwards:

| What was claimed | What was true |
|---|---|
| Silent save failures were React **hydration** | It was the **1Password autofill overlay** swallowing the click. `Escape` first, then Save. |
| Retitling a work is **safe** because `work_key` follows the title | That is exactly the **danger** — `work_key` is the join to **860 audiobook reviews**, and `WorkFields` deliberately refuses `title`/`authors` for that reason |
| *Who Goes Roar?* is publisher-authored | **Christie Hainsby** is the credited writer. MBI's house style omits the writer from the cover |

All three were cheap because a human was awake to push back. **Unattended, that
class of error compounds** — a wrong author becomes a wrong `work_key` becomes an
orphaned review. That is what an independent model on a separate budget is for.

## 3. Standing review checklist

When reviewing a change, check these specifically. Each one is a real bug this
estate has already shipped or narrowly avoided.

1. **Does it move `work_key`?** Derived from `title` + `authors`. It is the join
   to the sibling catalog's reviews. If a change can move it, the change must say
   what happens to the join.
2. **Does it invent a fact?** This codebase's whole character is refusing to
   assert what it cannot evidence — `openEnded`, `'at least N'`, `series_volume
   .source`, `cover_status`, `AudioSeriesMatch`. A change that fills a field with
   a plausible guess is a bug here even where it would pass elsewhere.
3. **Does a flag travel with its value?** Migration 0040's rule: `cover_status`
   moves with `cover_url` **or not at all**. Same shape recurs — a status written
   in a second request can fail while the first succeeded.
4. **Does it create one edition per barcode?** The Open Library **work-level**
   record aggregates every printing of every volume. One scan produced **6
   editions and 6 copies** of a phantom "Space Knight" on 2026-08-13. One barcode
   is one edition and at most one copy.
5. **Does it reuse the ONE implementation?** `normaliseTitle`, `splitAuthors`,
   `bookIdFromTitle` and `normaliseUniverseText` are each the single
   implementation and are **not** interchangeable. A second similarity function is
   how the sibling project shipped three wrong games.
6. **Is a silent failure distinguishable from success?** A cleared form looked
   exactly like a saved one on 2026-08-13 and lost a book.
7. **Does it need a migration?** If so, it does **not** ship unattended. See §5.

## 3a. Scope: the whole `heygabi-ai` workspace, not one repo

The owner put **`catalog-platform`, `boardbuddy` and `bookbuddy`** into one VS Code
workspace called **`heygabi-ai`** and asked for Fable to *"clear a lot of todos from
all of these projects."* So this brief is estate-wide. The work logs to read:

| Repo | Work log | State (2026-08-13) |
|---|---|---|
| `bookbuddy/library_catalog` | `docs/TODO.md` | the big queue — this session's |
| `bookbuddy/audiobook_catalog` | `docs/TODO.md` ⚠️ **gitignored, local only** | pending/decisions + the shared edit-any-detail ask |
| `boardbuddy/Board_Game_Catalog` | `docs/TODO.md` | **healthy** — clean tree, only **3** open items |
| `catalog-platform` | `docs/TODO.md`, `docs/PLATFORM.md` | CI gap + sequencing |

### ⚠️ The three repos converge — do not treat this as triple the work

`Board_Game_Catalog`'s **entire** open list is three items: **scan history**,
**splitting a shelf photograph**, and **the two thresholds worth re-measuring**.
Those are precisely the three that `PLATFORM.md` §7 Stage 1 already identifies as
**shared wheels**. So the same three fixes clear boardbuddy's backlog *and* unblock
the fork. Fix them once, in a form that ports.

⚠️ `audiobook_catalog/docs/` is **gitignored**, so its work log is a local-only
file. Read it, but do not expect to commit changes to it — and say so if you write
there, because a future session will not find it in git history.

## 4. Fable's work queue, in priority order

### 4.1 Review gate — continuous, highest priority
Review each change Opus produces before it deploys. Nothing else in this list
outranks an unreviewed deploy.

### 4.2 Design: edit-any-detail + an audit log
The largest and most dangerous piece. **Design only** — read
`docs/TODO.md` → *"Edit any detail, an audit log, and adding a book with no
author"* for the measured constraints before proposing anything. The short form:

- `work.authors`, `primary_author`, `work_key` are all **NOT NULL**
- `work_key` **contains the author on purpose** — `0001_init.sql` warns that
  title-only keys "collide across authors constantly"
- `work_key` is the join to **860 reviews** shared with `audiobook_catalog`
- ⚠️ "Flag for remediation" means the author arrives **later**, which moves
  `work_key` — harmless for a book added seconds ago, destructive for one with
  reviews. **The remediation path must know the difference.** That test is the
  design's core problem, not an afterthought.

⚠️ **This is a cross-repo rule.** `audiobook_catalog` needs the same feature and
shares the review store. Design the audit-log table and the `work_key`-move rules
**once, for both**. `catalog-platform/docs/PLATFORM.md` §2.2 governs what may
cross the boundary.

### 4.2a Design: an edition picker — ⚠️ #341 was NOT a one-off

Owner, 2026-08-13: *"the 341 editions wont be a 1 off so maybe add that to the
fable workflow."* They are right, and it is a structural gap rather than a chore.

**The case.** *He Who Fights with Monsters* book 1 exists as **two different
hardcover printings** the household owns — a dust-jacketed trade edition
(`9781638493457`) and a Target exclusive with a foil case wrap and no dust jacket
(`9781638494362`). Recording both required raw SQL, because:

⚠️ **`Copies.tsx` deliberately reuses an existing edition of the same format** —
`editions.find((e) => e.format === format)` — and its comment explains why: *"this
catalog learned that lesson the expensive way — `findEditionBySourceUrl` exists
because an importer created 83 duplicate editions by not checking."* That guard is
correct for its case and makes **"a second, different printing of the same
format"** literally unsayable through the UI.

**Why it recurs.** The catalog already holds Kickstarter hardcovers, Target
exclusives, B&N exclusives, signed printings, sprayed-edge variants and a
`collectors` edition kind on **43 rows**. The docs' own "Owned more than once"
section says outright that *"a Target edition and a Barnes & Noble edition are two
objects on the shelf."* The data model supports it; only the UI cannot express it.

**Design it as a picker, not a new form.** When recording a copy of a format that
already has an edition, the person needs to *choose* — this printing, or a new one —
and to see enough to tell them apart (ISBN, edition name, kind). ⚠️ Note the
overlap with **cover swap**: both are "several candidates exist for this book,
which one is this?" and they may want to be one component.

⚠️ Constraints that must survive the design:
- The **83-duplicate-editions** guard must still hold for importers and the scan
  path. Only a person choosing may create a same-format sibling.
- `edition_kind` has **no CHECK** on purpose (migration 0050) — do not add one.
- A wish must still create **no edition**: `reportFor` decides held-vs-wished by
  whether a work has any edition at all.

### 4.3 Platform work — genuinely suited to Fable

Checked 2026-08-13 across `catalog-platform/docs/`. Three items are cross-repo
design problems rather than local chores, which is where an independent model
reasoning over three codebases earns its keep.

| Item | Where | Why Fable |
|---|---|---|
| **Re-measure the matching thresholds** — `matchExistingTitle` and the 0.7 spine floor | `PLATFORM.md` §7 Stage 1, marked **"✅ Critical"** | The doc says outright: *"Fix once, or fix twice differently."* Books need a (title, author) key because titles collide across authors and Kindle rows carry ASINs not ISBNs. ⚠️ **Tonight's session hit this repeatedly** — the OL aggregate bug, bare series-line titles resolving to the wrong book, `Bizzy Bear` ×3. This is the highest-value shared fix in the estate |
| **Three of four repos deploy only from a human's laptop** | `catalog-platform/docs/TODO.md` §1 | A CI design that must preserve the two-lane deploy, and ⚠️ §1.2 warns *"do not copy the audiobook workflow wholesale"*. Needs judgement across four repos, not a template |
| **Scan history view** | `PLATFORM.md` §7 Stage 1 | Marked as porting cleanly. Pure scan-queue infrastructure — `scan_job.enriched`, mode/timestamp columns, paging |

⚠️ **Do NOT put "splitting a shelf photograph" on this list yet.** `PLATFORM.md`
says re-argue it and **measure first** — the games evidence ("vision read all 73
titles fine") does not transfer, because book spines are narrower and denser.

## 5. ⚠️ Hard limits — what Fable must not do

1. **No migration reaches production unattended.** Design it, review it, present
   it in the morning, apply on the owner's word. Nothing in the overnight plan
   except the edit-any-detail feature needs the schema at all.
2. **No guessing to unblock itself.** The things waiting on the owner are waiting
   because a guess would corrupt data. If a fact is unavailable, report it as
   unavailable — `docs/TODO.md` is full of examples of that being the right answer.
3. **No second implementation** of the four normalisation functions. See §3.5.
4. **Say what was not verified.** Every research report in this estate that was
   useful ended with an explicit "could not verify" list. A confident report with
   a silent gap is worse than a hedged one.

## 5a. ⚠️ Fable owns the app being down

Owner, 2026-08-13: *"if the app goes down fable will be in charge of saving it."*

So this is Fable's call, not the dispatcher's. **Recovery beats everything else on
the list** — drop the review queue and the design work until the site is back.

**How to tell it is actually down** — the site *looked* down to the owner once this
session and was fine from the server side, so check before acting:

```bash
curl -s https://library.heygabi.ai/api/health          # expect ok:true, database:up
```
⚠️ Do **not** trust `curl -o NUL -w '%{http_code}'` in Git Bash — it reports
`000` / exit 43 on a perfectly healthy host. Use PowerShell `Invoke-WebRequest`
for status codes, or read the body as above.

⚠️ A **200 on `/` is not proof the app works** — the shell can serve while the
bundle white-screens. Load a real page and check the console before declaring it
healthy, and remember the PWA can serve a stale bundle from cache on one device
while the server is fine.

**If it really is down, the rollback target is in the repo:**

```bash
tail -3 docs/deploys.log        # ISO<TAB>commit<TAB>holder<TAB>version
```
Each line is a commit that was live and the Cloudflare version it produced. Roll
back with `npx wrangler rollback --config apps/worker/wrangler.toml` (or redeploy a
known-good commit from that log). ⚠️ **Never kill a deploy mid-flight** — a killed
deploy can leave the live version out of step with the repo, which is the one
genuinely expensive state.

⚠️ **Write the incident up in §7 as it happens, not afterwards.** If Fable is
mid-recovery when the dispatcher next looks, the log is the only way to avoid two
runs both trying to fix it.

## 6. Progress log — ⚠️ both runs write, so neither collides

Owner: *"add to the fable doc that it needs to track its process in that doc so you
can be aware of where it is to help with overlaps too."*

**The protocol, and it is symmetric:**

| Run | Writes progress to | So the other can |
|---|---|---|
| **Fable** | **§7 of this file** | see what is claimed, in flight, done, or abandoned |
| **Opus** | `docs/TODO.md` | same, from the other side |

**Rules for both:**

1. **Claim before starting.** Append a line naming the item *before* work begins,
   not after. A half-done item nobody claimed is how two runs rebuild the same
   thing.
2. **Commit and push every claim and every result.** ⚠️ Both runs share one
   filesystem *and* one `origin`, so an uncommitted note is invisible. `git add`,
   commit, `git push origin main`.
3. ⚠️ **Write down what you could NOT solve** — the owner asked for this
   explicitly. Name the item, what was tried, and what would settle it. An
   unsolved item recorded with its dead ends is useful; an unsolved item silently
   dropped costs the next run the same search. *"Could not find"* is a result.
4. **Pull before deploying.** `deploy-guard.mjs` will refuse if the live commit is
   not in your tree, which is the same instruction enforced mechanically.

## 7. Fable's progress log — append below this line

Format, one line or short block per entry:

```
[ISO timestamp] CLAIM|DONE|BLOCKED|UNSOLVED|INCIDENT  <item>  — <detail>
```

⚠️ Do not rewrite earlier entries. An abandoned claim followed by a later
`UNSOLVED` is more useful than a tidy log, because the pair records that it was
attempted.

<!-- entries start here -->

```
[2026-08-12T23:24-07:00] CLAIM  §4.2 design: edit-any-detail + audit log + authorless add  — writing docs/info/edit-and-audit-design.md. Design only, no migration applied, no deploy. Constraint reading done (0001, 0008, 0040, WorkFields.tsx, works.ts, titles.ts, reviews.ts, identity-and-reviews.md, PLATFORM.md §2.2).
```

[2026-08-12T23:18-07:00] CLAIM  Re-measure the two matching thresholds (`matchExistingTitle`, 0.7 spine floor) — Fable 5. Plan: query both production D1s read-only (library works+editions+aliases, board game items), measure false-positive/false-negative rates of 0.34/0.7 and the 60% containment gate against real titles, propose a bare-series-name detection rule, write up at `catalog-platform/docs/info/matching-thresholds.md`. No deploys, no migrations.

[2026-08-12T23:45-07:00] DONE  Threshold re-measurement — write-up at `catalog-platform/docs/info/matching-thresholds.md`. Verdict: **keep 0.34 and 0.7 in both catalogs**; the numbers are shareable but only because thresholds are not what does the safety work — 79 book pairs and 15 game pairs of *different* things score exactly 1.00 (single-digit volume numbers are dropped by `titleWords`), so the structural gates (numbers-agree, author, fragment) must ship in BOTH matchers; each catalog currently holds gates the other lacks. Fragment attack: games matcher wrong on **162/354 (46%)** bare prefixes, library matcher **0/29** — the shared wheel must be shaped like `matchIndexedWork`, not `matchIndexedTitle`. ⚠️ Books' TRUE spine reads measured 0.50–0.75 (real shelf job 9, *My First* board books) — the games' "gap at 0.7" does not exist for books; the floor survives only as "nothing auto-ticks unless exact-ish". Bare-series-name rule proposed in §6 of the doc: refuse >1 edition per barcode and any OL `/works/` record as an edition source; review-only (never auto-tick) a title equal to a known series name with no volume number (18/341 real works legitimately carry such titles, so refusal would be wrong); standing audit for series-titled works with ≥2 editions. Side findings recorded: Korean titles normalise to `""` (two works share the empty key — CJK-collision path, fix is a migration); #258 carries 3 hardcover editions (edition-picker case or leftover dupes — eyeball it); library's `isConfidentMatch` lacks the games' `isFragmentOf` guard.

[2026-08-12T23:45-07:00] UNSOLVED  Not measurable tonight, recorded in §8 of the write-up: OL wrong-answer rate at scale (needs a live API sweep; 2026-08-09 isbn-ladder spot checks remain the only evidence); Kindle/ASIN name-only match rate (exactly 1 ASIN-only edition in production — no corpus); book spine read-rate on a full shelf (total corpus is one 7-spine job, so the 0.50–0.75 true-read population is n=6 and needs re-checking after more scans — the "split the shelf photo" question stays open for the same reason).

[2026-08-13T00:15-07:00] DONE  §4.2 design — `docs/info/edit-and-audit-design.md`, design only, nothing applied. The key decision: an authorless book stores sentinel `?unknown` in `authors`/`primary_author` and gets `work_key = <titlefold>|?unknown` — the `?` cannot survive `normaliseTitle`'s [a-z0-9 ] fold, so the provisional key can never equal a real one, and `reviewDocFor` refuses to ever stamp it onto a review doc. **Remediation is therefore safe by construction, not by measurement**: zero docs can carry a provisional key, so filling in the author later is always a free key move. No `work` rebuild (0008 measured data loss on both escape pragmas; `work`'s child closure is essentially the whole DB) — `toWork()` maps sentinel→null so the app type is `string|null` and the compiler finds every reader. Established-book title/author edits get a two-sided ceremony: browser live Firestore check (both queries) + server evidence floor (`reviews_seen_*` columns, rating evidence, prior key-move audit rows), restamp-docs-FIRST-then-PATCH ordering so a half-done move degrades to legacy-query visibility and is idempotently re-runnable. Audit log = `change_log` (per-field rows, batch_id, no FK on entity_id so deletes stay accountable, old/new as NOT NULL JSON, DECISION_MODES `changed_how`), one DDL for both catalogs per PLATFORM §2.2 — audiobook side applies it in its own store when it gains an editor; today its git-tracked overrides file already is its audit trail. ⚠️ Two live gaps found while reading: `PATCH /api/works/:id` accepts `title`/`authors` TODAY with no guard (UI patch object is the only protection), and `updateEditionSchema` accepts `isbn13`/`asin` patches — both closed in the design, worth knowing about before it ships.

[2026-08-13T00:20-07:00] CLAIM  Review gate (§4.1): commit `7a3b749` — server-side refusal of `title`/`authors` on `PATCH /api/works/:id`. Adversarial review before deploy; not deploying either way, dispatcher deploys on the verdict.

[2026-08-13T00:50-07:00] DONE  Review of `7a3b749` — **verdict SHIP**, verified rather than approved. (1) Caller sweep re-done independently: every `api.updateWork` call in `apps/web` (`WorkFields.tsx`, `Enrich.tsx`, `catalog-add.ts:194`) sends neither field; `covers.ts` and `applyFinding` call `@lc/db`'s `updateWork` *below* the route, unaffected; all 48 `scripts/*` checked — only `import-ebooks.mjs` speaks HTTP and it POSTs `/api/ingest/ebook`, and `fix-title-case.mjs` (the one title-changer) writes SQL via wrangler with its own `normaliseTitle`-equality refusal; `audiobook_catalog`, `boardbuddy`, `catalog-platform` contain no `api/works` caller at all. (2) 313 tests + typecheck re-run, clean. (3) ⚠️ Exercised live on the dev worker (13 probes, work 53, then reverted, worker stopped by name): `title`/`authors`/both/`title:null` → 400 `frozen_field` with correct `fields`; **atomicity held** — a `subtitle` sent beside `title` did not land; cased `"Title"` passes the guard but Zod strips it and the key does not move (no bypass); array/string/number/null/malformed bodies all fall through to clean Zod 400s. (4) Whole-patch refusal is right: a 400 that had also mutated state would make resend-without-the-field re-apply half a patch, and a 200-with-warnings is the partial-success lie this estate keeps hitting. (5) The guard is the interim form of the design's own §5.2 gate — ⚠️ when the key-move ceremony builds, this 400 block becomes the 409 `key_move_requires_check` branch **in place, on this route**; do not read the comment's "opens its own guarded path" as licence for a second endpoint.

[2026-08-13T00:50-07:00] DONE  `updateEditionSchema` isbn13/asin question (deliberately not touched tonight — confirmed correct): ⚠️ **the Editions "Edit" UI legitimately patches ISBN today.** `Editions.tsx` `EditionForm` (lines ~307–321) sends `isbn13`/`isbn10`/`asin` unconditionally on *every* save, and `AddWork.tsx`'s failure path ("Book added, but that ISBN could not be recorded — add it from the book page") depends on exactly that form. A presence guard like tonight's would 400 every edition edit in the app. Recommend: freeze edition identifiers only as part of the edit-and-audit build, in the same commit that makes `EditionForm` delta-only, with the owner deciding whether null→value (adding a missing ISBN) stays allowed while value→value becomes delete-and-recreate. Risk asymmetry supports waiting: a wrong ISBN is one visible, UNIQUE-guarded row; a moved work_key silently orphans ~870 reviews.

[2026-08-13T00:50-07:00] UNSOLVED  Pre-existing, out of tonight's scope: `updateWorkSchema` still silently strips *any* unknown key — `{"Title": …}` or a misspelled field returns 200 having changed nothing, the exact strip-lie the estate documents. Fixing it is a `.strict()`-shaped decision across every patch schema, wider than this commit; recorded rather than smuggled in.

[2026-08-13T00:30-07:00] CLAIM  Estate-wide improvement/feature survey — Fable 5. Reading all four repos' TODO/PLATFORM/info docs + querying both production D1s read-only; writing ranked proposals to `catalog-platform/docs/info/improvement-proposals.md`. Survey and proposal only: no deploys, no migrations, no app-code changes.

[2026-08-13T01:30-07:00] DONE  Estate-wide improvement survey — write-up at `catalog-platform/docs/info/improvement-proposals.md` (+ its README index row). All counts read live from both production D1s 2026-08-13. Top six, ranked: (1) implement the one-barcode-one-edition guard — specced in matching-thresholds §6 and **measured as still absent from code** while scanning resumes today; (2) rescan-is-a-question — 71 physical editions have no ISBN and the promised "scan will fill it in later" path does not exist: `catalog-add.ts` writes a new owned copy unconditionally and a second same-format edition (residue already live: #139 holds an OL hardcover beside a `manual` ISBN-less one), fix reuses the shipped preorder-prompt shape and should merge with the edition picker; (3) rehost 151 OL/Google cover hotlinks into R2 (108+43 measured, vs 33 self-hosted; bucket live since 0ab1e18e); (4) author-string canon + dedupe report — `Make Believe Ideas` vs `Make Believe Ideas  Ltd.` and `SAMG` vs `SAMG Entertainment` are live, the latter the only thing preventing a full CJK work_key collision; merges one-at-a-time behind the audit ceremony; (5) stand up the index Worker games-first — two hand-built library↔audio bridges already exist and drift between script runs, zero bridges to 836 games; (6) refresh PLATFORM.md's stale §1/§8. Remove/simplify: games `play` and `sleeve_requirement` tables are **dead schema** — 0 rows and no write path anywhere in that repo, only export.ts reads them; `user_item` ratings 0 rows with live code = watch. Do-not-build recorded with reasons (lack-counts, auto-accept of findings despite the 798/798-accepted record, bulk author merge, book shelf-splitting, DB merge, Serper, ISBN-from-research, bulk cover_status stamping). No deploys, no migrations, no app code touched.

[2026-08-13T01:30-07:00] UNSOLVED  Survey items not assessable tonight (§7 of the write-up): Firestore live reads (no service account held, by policy); whether #163/#203's twin hardcovers are two real printings or a scan-resolved sibling printing (needs the shelf); games ratings UI reachability (write path exists, no browser driven); R2 cache rule on bookcovers.heygabi.ai (dashboard not opened); `scan_job.enriched` blob completeness for the history view (rows counted 32/30, blobs not sampled).

[2026-08-13T00:15-07:00] UNSOLVED  Five items in the design's §9, headline two: (1) title-edit doc-id drift — a retitled book's future library-side review lands under a new slug beside the person's old doc; tried stable ids (impossible, `bookIdFromTitle` built every production id) and id rewrites (delete+create of other people's docs, breaks audiobook `getReviews`); settles with a measured retitle-of-reviewed-book frequency or a dedupe-on-write in `reviewDocFor`. (2) the carry procedure depends on Firestore's shape-only rules (no `request.auth`) letting any client restamp anyone's review doc — if `reviews` rules are ever hardened the restamp breaks silently; settles with a PLATFORM.md decision on whether `reviews` ever gets auth rules (and then the carry needs the service account this estate refuses to hold). Also unverified by execution: the migration SQL was never dry-run against local D1 (design-only task) and the restamp→PATCH interleave has never been exercised.

[2026-08-13T00:01-07:00] CLAIM  Build the edit-and-audit code (design §7 "code changes riding along") — Fable 5. Owner approved all four open questions; migration 0120 already written and applied to LOCAL D1 by Opus (not production — Opus migrates+deploys after review). Priority: (1) @lc/core sentinel+tests, (2) @lc/db works.ts sentinel mapping + actor + change_log batch writes + NEEDS_AUTHOR, (3) worker key-move gate + reviews-seen + provisional refusals, (4) web UI. Not deploying, not migrating remote. Committing to main only.

[2026-08-13T07:45-07:00] DONE  One-barcode-one-edition guard — ALL THREE tiers of matching-thresholds §6, commits `52c559f` (tier 1) + `b5ee502` (tiers 2+3), pushed. Tier 1 in `@lc/isbn`: `lookupOpenLibraryByIsbn` refuses work-level records (/works/ in url or an OL…W identifier) and any answer carrying >1 distinct ISBN-13; same distinct-ISBN refusal on the Google rung; `workKeyForIsbn` (redirect-follow can land on a work JSON) returns the work key with `edition: null` instead of dressing the aggregate as a printing; `editionsOfWork` drops /works/ entries; refusals are throws prefixed `refused:` so they land in the rung trace distinguishable from "nothing found", and `resolveBarcode` says the true reason on the line via `wasRefused`. ⚠️ Live probe DURING the build changed the plan: OL now answers incident ISBN …641696 with a clean single-ISBN *edition* record titled bare "Space Knight" (…061/…078 answer `{}`), so tier 1 alone provably would NOT stop today's recurrence — tier 2 was therefore built in the same sitting rather than deferred: `isBareSeriesTitle`/`foldSeriesNames` in core matching.ts (digit test on the folded key; review-only because 18/341 real works are legitimately series-titled — a test pins that stance), `listKnownSeriesNames` in @lc/db (the spec's three-table union), `warnBareSeries` applied at all three resolution-landing sites in scan-jobs.ts plus a bare-series variant of the spine "check this one" detail. Tier 3: `npm run audit:series-aggregates` (read-only, exit 1 on hits — series-titled works with ≥2 editions). Verified by execution: 334/334 tests (11 isbn guard tests using the six real incident ISBNs incl. rung-degradation, 5 core predicate tests), typecheck clean, audit script exercised against local D1 (flagged #93 Beneath the Dragoneye Moons, 2 editions — the designed eyeball case). npm test's glob now includes packages/isbn/test/.

[2026-08-13T07:50-07:00] CLAIM  Task 2 — finish the edit-and-audit build (design §7 ride-along): worker catalog.ts key-move gate (replaces the blanket 400 in place, per my own review note on 7a3b749) + `POST /works/:id/reviews-seen` + delete `__row__` logging; reviews.ts `/observed` refusal (guard 3); web UI (edit title & author panel, Add-without-author, Needs→Author, card mark, Changes panel, reviews-held message). PLUS the dispatcher's added scope: patch schemas reject unknown keys with a 400 naming the field — client sweep first, all-or-nothing across work/edition/copy. Dispatcher's closures accepted: PLATFORM.md §4a settles the rules posture (read before writing the carry), 0120 dry-run verified, doc-id drift accepted as a known wart (comment only, no dedupe-on-write).

[2026-08-13T08:55-07:00] DONE  Task 2 — the edit-and-audit build is code-complete against the design's §7 table. Commits `6ba27bd` (server) + `eee2d8d` (web), pushed. Server: PATCH /works/:id key-move gate replaced the blanket 400 in place — every §5.2 refusal exercised live against local D1 (21 probes: 409 requires_check/stale_key/evidence_mismatch/reviews_would_detach, 400 inconsistent attestation, free provisional moves, ceremony move with 'reviews restamped: 3' note, carriedKeyMoves evidence leg, deletion __row__ surviving the row); POST /works/:id/reviews-seen; GET /works/:id/changes; POST /works + DELETE now pass the human actor; reviews.ts /:id/observed 409s on provisional (guard 3) and the bulk sweep drops crafted sentinel keys. Dispatcher's added scope DONE all-or-nothing: updateWork/Edition/Copy schemas are .strict() after a client sweep found every PATCH body modeled (Editions' unconditional isbn13/isbn10/asin included — they are modeled fields, so .strict() does not break edition saves); one test that pinned the workId strip-behaviour was updated to pin the refusal instead; new test asserts unrecognized_keys names the offender on all three. Web: EditTitleAuthor (live check keeps doc ids, Save disabled until it resolves, restamp-Firestore-FIRST then PATCH+keyMove, provisional light mode, ApiError.detail surfaced, doc-id drift documented as the owner's accepted wart per the dispatcher's instruction); Changes panel; Reviews held-state + reviews-seen piggyback; Add-without-author as an explicit button on scan lines and AddWork (mode survives the preorder prompt's re-run); /works/match authors now optional = provisional-key match (probed live, 4 cases); Needs→Author option + third facet; "No author" card mark; null-authors rendering across WorkList/Shelf/WorkPage. 335/335 tests, typecheck all workspaces, vite build clean.

[2026-08-13T09:20-07:00] DONE  Sentinel containment sweep — found while writing the previous UNSOLVED and fixed instead of logged: seven queries under packages/db SELECT `w.authors` raw, bypassing `toWork`, and would have printed the literal `?unknown` as an author's name (wishlist, details queue, auto-applied list, CSV export, watches list, related works, series ladder, crowdfunding provenance). All now fold it at the SQL boundary — `NULLIF(w.authors, '<sentinel>') AS authors`, the same mapping toWork does — and every touched interface plus its api.ts mirror is honestly `string | null`. Deliberately NOT folded: the JSON backup export (raw rows are the point of a backup), `listWorksForMatching` (the matcher needs a string; `foldAuthorNames` folds the sentinel to 'unknown', same fate as a real "Unknown" credit), and `aliases.ts`'s internal redundancy check. Also read the FIRST REAL change_log corpus (19 rows / 6 batches, production, read-only) per the dispatcher's pointer and reworked the Changes panel on what it showed: grouped by batch_id (flat, the merges read as 19 unrelated lines; grouped, six events — the design's §4.1 claim tested against real data), batch ids rendered verbatim never parsed (they are hand-written slugs, not UUIDs), empty-string olds spelled `""`, and notes rendered verbatim because 'KEY MOVE, no restamp performed' IS the record. Confirmed my keyMoveEvidence LIKE ('reviews restamped: %', excluding ': 0') does NOT count those two un-restamped moves (works 141/328) as carried-move evidence — correct in the safe direction, they prove nothing about reviews existing. 335/335, typecheck, build clean.

[2026-08-13T08:55-07:00] UNSOLVED  What was NOT verified by execution, explicitly: (1) the browser ceremony end-to-end — the restamp needs a signed-in Firestore session and a book with live reviews; a curl cannot fake either. The server half of every refusal it can hit WAS exercised; the untested seam is countReviewDocs→restampReviews→PATCH interleaving in a real browser, which the design's §9.5 already named as the build task's residue. Settled by: one attended retitle of a low-stakes reviewed book on the dev lane before this deploys to prod, watching the network tab. (2) The Changes panel and held-state UI were verified by typecheck+build+the real corpus's shapes, not by driving a browser. (3) Sentinel keys in the bulk /observed sweep are dropped silently — `considered` still counts them; if that reads as a lie in practice, subtract dropped from considered (one line in reviews.ts). (4) A merge's reparenting rows live under the edition/copy entities and the merged-away work's id, so the SURVIVING book's Changes panel does not show its own merge — real case in the corpus (291 and 333 show nothing). A cross-entity event view is a separate read with its own paging (listChangesForEntity's header says so); not smuggled in. (5) The details queue's empty state (queue now 0/62): the page's own empty rendering was not driven in a browser; listWorksNeedingDetails returning [] renders whatever DetailsQueuePage always rendered for no rows — eyeball it after deploy.

[2026-08-13T09:45-07:00] CLAIM  ISBN backfill for the ~71 physical ISBN-less editions — Fable 5. Determinable-only writes to production D1 (each with a change_log row, entity='edition', changed_how='auto', source URL in note); slipcase volumes SKIPPED by rule; ebooks out of scope except ASIN-determinable cases. Primary deliverable includes the needs-the-barcode shelf list. No deploy, no migrations.

[2026-08-13T10:35-07:00] DONE  ISBN backfill — **6 written, 1 settled without an isbn13, 64 need the barcode** (of the 71 physical; 7 slipcase volumes skipped by rule, 117 ebooks out of scope). Written, each with a change_log row (batch `isbn-backfill-20260813`, changed_how='auto', source URL in note, checksums validated, UNIQUE index pre-checked): the four Year of Sanderson premium hardcovers (9781938570322/339/377/391 — stated on Dragonsteel's own product pages, edition pinned by edition_name) and Untapped HC 9781637663295 + PB 9781637663301 (only known Mountaindale printings; no Kickstarter variant exists — checked). Settled: WoR Signed Leatherbound is a TWO-VOLUME set (Vol 1 9781938570308 / Vol 2 9781938570315, Dragonsteel page the row already cited) — one row cannot carry two ISBNs, so both went into edition_name slipcase-style and isbn13 stays NULL on purpose. ⚠️ Traps found and dodged: Fires of December's tier copy is the Dragonsteel premium illustrated edition from the Hoid's Storybook campaign — the ISBN searches surface (9781250462657) is the TOR RETAIL edition, a wrong fact; Unmapped has a public ISBN (9781637663608) but NO source states its binding and the sibling format's ISBN is unfindable — one scan settles both rows; Dinosaur Dance! has exactly two Little Simon candidates (9781481480994 board book vs 9781665907903 oversized Lap Edition — size decides). Shelf list at `docs/isbn-barcode-worklist.md`, grouped by author, incl. the Realmkeeper structural warning (16 rows = 8 physical omnibus volumes; a volume ISBN can land on only one row — slipcase treatment needed). Verified after the batch: all 7 rows re-read, 7 audit rows counted. Anonymous Google Books is fully dead (429, quota 0/day) — used the project's own key from .dev.vars for 3 lookups. No deploy, no migrations.

[2026-08-13T11:05-07:00] CLAIM  Rescan-is-a-question (survey item #2; owner blocked on rescanning the slipcase volumes) — Fable 5. Scope: when a scanned barcode's ISBN is NOT on file but the work IS (the `existing.work` branch of `catalog-add.ts`, which today silently writes a new edition + copy), stop and ask, preorder-prompt shape. Four outcomes: fill the ISBN-less edition's isbn13 (the owner's case, no new rows, link the NULL-edition copy when unambiguous), new printing (edition+copy), extra copy, different book. UNIQUE isbn13 conflicts answered as a named 409 with the holding edition, client offers the slipcase note treatment (Realmkeeper's 16-rows-8-volumes trap must not dead-end). Fold-ins: `existingEditionId` on owned scan lines so "Add 2nd copy" links `copy.edition_id`; change_log audit rows (with actor) on edition/copy create/update/delete, which today log nothing. No deploy — dispatcher ships.

[2026-08-13T12:20-07:00] DONE  Rescan-is-a-question — commits `634f4d8` + the follow-up, pushed, NOT deployed (dispatcher ships). **The owner can rescan the slipcase volumes the moment this deploys.** What they will see: scanning a barcode the catalog has never seen on a book it holds now raises a prompt in the pre-order shape — "“Title” is already in the catalog, but the barcode you scanned (978…) is not on any of its printings." — with one PRIMARY button per ISBN-less physical printing ("This is the [name · format] already on the shelf — record this ISBN on it": fills `edition.isbn13`, links the one unlinked owned copy, creates NOTHING), plus "I have two of it" (copy + ISBN), "a different printing I own" (edition + copy, the #341 case), "a different book" (new work), "never mind" (writes nothing). A fill that hits the catalog-wide UNIQUE index answers as `409 isbn_taken` NAMING the holder, and the client offers the slipcase treatment — shared ISBN into this row's `edition_name` — so Realmkeeper's 16-rows-8-volumes shape cannot dead-end. `rescanQuestionFor` returns null for works with no physical presence, so the paperback-of-an-ebook attach (the commonest add) costs no extra tap. The pre-order question composes with every answer including fill (the variant-cover pre-orders are recorded against ISBN-less printings, so "this is the printing on file" and "this is the pre-order arriving" are usually both true — fill+arrived does both writes). Fold-ins: owned barcode lines carry `existingEditionId` so "Add 2nd copy" links `copy.edition_id` (the 67%-NULL defect's minting point); the ordinary attach links its copy to the edition it creates; ALL edition/copy mutations now write per-field/`__row__` change_log rows with an actor in the same db.batch (works.ts pattern — they logged nothing before). Verified by execution against local D1 via wrangler dev, 14 probes: fill → exactly {isbn13, editionId} audit rows changed_how='human'; sibling fill → 409 naming holder title+edition_name on both PATCH and POST; row re-saving its own ISBN → 200 no-op with ZERO audit rows (Editions.tsx resends identifiers every save — a false diff there would have spammed the log); barcode of the just-filled ISBN → owned line with existingEditionId set; linked copy create; delete `__row__` rows carrying the whole row. 346/346 tests (11 new pinning: ebook rows never offered, no question without physical presence, wanted-only works excluded, link only when unambiguous), typecheck all workspaces, vite build clean, wrangler killed by name, test rows deleted.

[2026-08-13T12:20-07:00] UNSOLVED  Residue, named per rule: (1) **The browser seam was not driven** — RescanPrompt/IsbnTakenPrompt rendering and the ScanLines re-run wiring are verified by typecheck+build+the same-shaped preorder precedent, not by a browser; the server half of every write they can make WAS exercised. Settled by: one attended scan on the dev lane before the owner does the shelf run. (2) A Realmkeeper volume's scan resolves to ONE of its two books; the other row never gets a scan event, so its shared-ISBN note stays a hand edit (worklist doc says so now, with the click path). (3) Edge-of-edge intent drop: fill → preorder-arrived → fill collides → person switches to the note → the arrival flip is not re-asked; the pre-order stays visible on the wishlist, so it is recoverable, but it is a second tap nobody prompts for. (4) `catalog-add.ts` is ~500 lines and now carries two prompt protocols; the §4.2a manual edition picker (Copies.tsx) should reuse `@lc/core/rescan.ts` + RescanPrompt rather than growing a third — not built tonight, one clean commit beat two half ones. (5) AddWork's per-save-work behavior (its header records the deliberate non-dedup decision) still creates a new work for a hand-typed title that collides — out of scope, recorded as untouched.

[2026-08-13T13:05-07:00] CLAIM  Surface the illustrator (0130 ALREADY APPLIED both lanes — no migration work) + book number on the detail page — Fable 5. Scope: @lc/db works.ts (column, toWork, create/update + audit diff), @lc/core schemas (optional nullable FREE field — no key ceremony, not frozen; workKeyFor signature untouched), web credit line on the book head + edit via WorkFields; work id as a quiet, selectable, click-to-copy `#N` on the detail page head. No deploy — dispatcher ships.

[2026-08-13T12:50-07:00] DONE  Illustrator surfaced + book number on the detail page — one commit, NOT deployed (dispatcher ships). **Illustrator**: `works.ts` carries the column through WORK_COLS/toWork/create/update with a per-field audit diff row (`consider('illustrator', …)`), `createWorkSchema` gains `illustrator: optionalText` (updateWorkSchema inherits via `.partial()` — free field, no ceremony, NOT frozen, and `.strict()` still names any misspelling); `workKeyFor` untouched, and a new core test pins `workKeyFor.length === 2` as the tripwire against widening it. The PATCH route needed ZERO changes — an illustrator-only patch computes `newKey === oldKey` and takes the plain path, which is the design working rather than a gap. Web: credit renders as "Illustrated by {name}" directly under the byline (serif, smaller, muted — the author stays primary when both exist), null renders NOTHING; editable in **WorkFields** ("About" → Edit), placed under Subtitle with the free-field reasoning in the hint, NOT in EditTitleAuthor — putting a free field behind the ceremony panel would teach the wrong lesson about what is dangerous. **Book number**: `#N` above the title in the work head — `<code role="button">` (NOT `<button>`: several browsers make button text unselectable), `user-select: all` so one tap selects the whole token, click writes it to the clipboard with an aria-live "copied", monospace/muted/0.78rem so it reads as a catalog number and never competes with the title. Above rather than corner: a corner placement collides with title wrap on a phone. No additional placements — nothing else was asked and ids would scatter. Verified by execution: 347/347 tests, typecheck all workspaces, vite build clean; live against local D1 (wrangler dev, killed by name after): PATCH set/clear round-trip with key never moving, audit rows changed_how='human', blank→null fold, no-op resave logs zero rows, POST /works stores it, probe work deleted. ⚠️ One probe body guessed `series:null` against work 53 and cleared its real series — restored immediately (change_log rows 30–32 + the restore are the probe's audit trail, left in local change_log because an audit log that gets tidied is not one).

[2026-08-13T12:50-07:00] UNSOLVED  Residue, named per rule: (1) the browser surfaces (credit line, WorkFields input, the copy tap) are verified by typecheck+build, not by a driven browser — one attended look at prod #269 after deploy settles it; (2) `navigator.clipboard` needs a secure context — on https it is fine, and the `user-select: all` selection is the designed fallback, but the copy tap itself was never exercised in a real browser; (3) local D1 does not contain #174/#269 (production ids), so the two backfilled credits were verified in production by the dispatcher, not re-verified here; (4) the illustrator is deliberately NOT on WorkList/Shelf cards or the CSV/JSON exports — cards have no room for a second credit and nobody asked for export changes; noted so its absence there is read as a decision, not an omission.

[2026-08-13T13:40-07:00] CLAIM  Three items, one run — Fable 5. (1) `detailGaps` two-column fix (dispatcher's mid-run report: sort set + display NULL was invisible to the queue; production backfilled, the LOGIC still wrong) — one predicate + tests, threaded through `listWorksNeedingDetails`/`gapSummary`/`gapsFor` so the SQL readers pass the display column too. (2) Record delete button: GET deletion preview + DELETE refusal when copies record property (409, no override flag), cascade casualties (editions/copies) logged as their own `__row__` audit rows in the delete batch, web Danger panel on WorkPage showing what dies BEFORE it dies. (3) Cover swap: port of boardbuddy's CoverPicker idea — GET /works/:id/covers assembling candidates from current + edition covers + change_log coverUrl history (R2 objects are content-addressed and never deleted, so history is still live) + derived OL guesses; picking writes through the EXISTING verified PUT. No deploy — dispatcher ships.

[2026-08-13T15:10-07:00] DONE  All three, committed and pushed, NOT deployed (dispatcher ships): `96b2a48` (gaps), `c3f4e80` (delete), `6672b3f` (cover swap). 360/360 tests, typecheck all workspaces, vite build clean, wrangler killed by name, every probe row deleted from local D1. **Gaps fix**: `seriesIndexIncomplete(sort, display)` is the ONE predicate — `detailGaps`, `gapSummary`'s tally (its SQL now selects the display column) and `gapsFor` all read it; tests pin #341's exact shape, the whitespace display, and the mirror state (display set, sort null = files nowhere). ⚠️ Deliberate interaction, decided not stumbled: `applyFinding` still writes sort only (display quotes the COVER; research read a web page), so a research-filled volume number now keeps its gap open until a person quotes the cover — both applyFinding messages say so instead of claiming done, and 'already volume N' stops re-buying meanwhile. **Delete button**: `copyBlocksDeletion` in core — everything except a plain wish blocks (owned/lent/preordered/borrowed/sold + signed-whatever-status), NO force flag; DELETE recomputes the report server-side and 409s with the fresh report; `deleteWork` now logs the cascade casualties (one `__row__` row per edition and copy, same batch_id, note 'cascade: work #N deleted') — the four raw-SQL deletions in production destroyed their subtrees unlogged, this button cannot. Exercised live: owned copy → 409 naming it; signed wish → 409; copy removed → 200; log shows work+edition+copy rows under one batch. **Cover swap**: `assembleCoverCandidates` pure in core (tested), queries in db/covers.ts, GET /works/:id/covers, CoverSwap grid inside CoverPanel ("Choose from known covers"); candidates = edition covers (richest description wins the URL tie) + change_log coverUrl history ("Previous cover · until date") + unexplained current (never swap-away-only) + derived OL guesses last (`?default=false` so a wrong guess 404s and the card is dropped; recorded candidates that fail render "image no longer loads" instead — a guess failing is noise, a fact failing is information). Applying a pick goes through the EXISTING verified PUT — dead candidate → 422, nothing stored. Probe found one wart, fixed with a test: a swap-back deduped into its own history card and read "Previous cover · in use"; now reads "Current cover".

[2026-08-13T15:10-07:00] UNSOLVED  Residue, named per rule: (1) **Neither new browser surface was driven in a browser** — DeleteWork and CoverSwap are verified by typecheck+build+live probes of every server path they can hit; the untested seam is rendering and the tap flow. Settled by: one attended look on the dev lane — open a wishlist-only book's Delete panel and any multi-edition book's cover grid. (2) **Merge-into-another-work is NOT built** — the delete confirmation names the alternative in words ("its copies belong on the right record") but offers no button; re-homing a copy today means edit its work assignment by hand or SQL. My scope-creep verdict is in the run report: worth building, separately, with a design pass over the ~13 child tables. (3) Google Books cover candidates not offered — no derivable URL without an API call + key; the OL guesses and recorded facts cover the realistic cases; if wanted later it belongs in Enrich's lookup, not the picker's assembly. (4) The cover history only reaches back to migration 0120 (change_log's birth, 2026-08-13) — covers replaced before then left no trail, so old R2 objects from that era are findable only by listing the bucket. (5) `gapSummary`'s seriesIndex 'filled' now honestly shrinks — the queue page's tally will show more missing volume numbers than before the fix; that is the 22-works truth becoming visible, not a regression; worth one sentence to the owner before they read the queue page.

[2026-08-13T15:45-07:00] CLAIM  §4.2a manual edition picker — Fable 5. Scope per my own residue note on `634f4d8`: reuse `@lc/core/rescan.ts` + the RescanPrompt pattern, NOT a third protocol. Four surfaces: (1) AddCopy stops and asks when a same-format printing is already on file (the #341 "second, different printing of the same format" case becomes sayable; "not sure" still records an unlinked copy); (2) "Which printing?" on copy rows — the 172-unlinked-copies write, `PATCH /copies/:id {editionId}`; (3) "Add a printing" in the Editions panel — EditionForm gains create mode, ISBN never required; (4) "no barcode on the object" recorded as an observed fact in `edition_name`, one spelling in core matching the two owner-verified production rows. Server: POST /editions refuses a same-format sibling carrying NOTHING to tell it apart (409 naming the sibling — the #139 residue shape); copy↔edition work-match check on the accessories precedent. isbn_taken 409 reused as-is, no second error shape. No deploy — dispatcher ships.

[2026-08-13T16:50-07:00] DONE  §4.2a manual edition picker — commit `908ec55`, pushed, NOT deployed (dispatcher ships). 371/371 tests (11 new), typecheck all workspaces, vite build clean, wrangler killed by name, all probe rows deleted through the API's own delete paths. **Reuse verdict, asked for explicitly: reusing `@lc/core/rescan.ts` held up without a fight** — the picker functions (`printingCandidates`, `newPrintingNeedsName`, `blankSiblingOf`, the no-barcode note family, `printingQuestionText`) sit beside `rescanChoices` and share its types; the ONE place literal reuse was wrong and was not forced: `RescanAnswer.fill` MEANS "write this ISBN onto that row", which a barcode-less answer cannot mean, so the picker answers are their own small type and `EditionPickerPrompt` is RescanPrompt's sibling in the same file rather than the same component — same contract (nothing written before an answer; every button names its writes; never-mind writes nothing), same label builder (`printingLabel` in lib/rescans.ts now labels BOTH prompts' buttons — name · kind · format · ISBN), same question style. What shipped: (1) AddCopy stops and asks when the chosen format already has a printing — the silent `editions.find(format)` reuse that made #341's second hardcover unsayable is gone; "not sure" records the copy honestly unlinked; first-of-format still creates silently (nothing to confuse it with). (2) "Which printing?" / "Change printing" chip on every copy row → `PATCH /copies/:id {editionId}` — the 172-unlinked-copies write, no barcode needed. (3) Editions panel "Add a printing": EditionForm serves create too — format explicitly chosen (no paperback default for a hand-described row), NO identifier required, `source:'manual'`. (4) "No barcode printed on this copy (owner-verified)" as a tick whose state IS the edition-name text — spelling read from production rows ed450/ed470 and pinned by test, so the fact greps identically forever. Server: `409 indistinguishable_printing` on POST /editions for a same-format sibling carrying NOTHING to tell it apart (isbn_taken body convention, holder named — the #139 residue shape, closed at its last minting point); `CopyLinkError` in @lc/db refuses a `copy.edition_id` naming another book's printing (400/404, the accessories `assertCopyBelongs` argument — the FK never checked work identity and the picker multiplies the writers). Exercised live against local D1, 13 probes: blank sibling 409 naming holder; name/publisher/identifier each defuse it; first-of-format passes; link write logs exactly one `editionId` audit row changed_how='human'; no-op relink logs ZERO rows (chip re-picks are free); cross-work link 400, ghost edition 404, on both POST and PATCH; isbn_taken regression intact with holder named; note spelling round-trips the real PATCH verbatim.

[2026-08-13T16:50-07:00] UNSOLVED  Residue, named per rule: (1) **No browser surface was driven** — EditionPickerPrompt, NewPrintingForm, the copy-row chips and EditionForm's create mode are verified by typecheck + build + live probes of every server write they can make; the untested seam is rendering and the tap flow. Settled by one attended pass on the dev lane: open any multi-edition book, tap "Which printing?" on a copy, and add one named sibling via Record-a-copy. (2) **A copy's link can be moved but not cleared** — the picker offers other printings, not "this copy names no printing after all"; `editionId: null` is API-reachable but has no button. Deliberate: no owner ask names it, and an unlink with no reason reads like the blank the whole feature exists to close. (3) **PATCH /editions can still blank a named sibling into indistinguishability** (clear the name of one of two same-format rows) — the guard covers creation only; edit-path guarding needs the delta-only-form work my `updateEditionSchema` review note already scoped, not smuggled here. (4) The blank-sibling guard is route-level; `@lc/db createEdition` callers bypass it (ingest keeps its `source_url` dedupe, the scan path always carries an ISBN) — a future direct-db caller writing blank same-format rows would not be refused. (5) The no-barcode spelling keeps "(owner-verified)" verbatim even if the other household member ticks it — one greppable spelling beat actor precision, and change_log records who actually ticked.

[2026-08-13T17:20-07:00] CLAIM  Shared index Worker, games first (survey §1.5, PLATFORM §5) — Fable 5. ⚠️ Session limit resets in ~25 min; expected to be killed mid-run, so: DESIGN FIRST at `catalog-platform/docs/info/index-worker-design.md` (commit that alone as a good outcome), then build in Board_Game_Catalog (no test script there — typecheck only, say so). Reading the two drifting bridges (review-key backfill + audiobook_holding/0110) as the problem statement. Hard question is the join key: normaliseTitle folds non-Latin to "", games have no author — key must not pretend games are books. No deploy. Also: refresh PLATFORM.md stale §1/§8 if cheap.

[2026-08-13T17:40-07:00] DONE  Index Worker DESIGN — `catalog-platform/docs/info/index-worker-design.md`, commit `78a7997`, pushed; README row + PLATFORM.md unstale (§1 dated update block, §8 Q1 closed as heygabi.ai) in `bf70e55`. The key decision, so nobody re-derives it: **components not a concatenated key, joined at two tiers** — `work_fold` (title_fold|creator_fold, computed by the index on write) exists for BOOK rows only; games rows carry `work_fold = NULL` by design forever, because a game is never the same *work* as a book; games join cross-format ONLY at the `universe` tier (resolved on write from universes.json). ⚠️ Empty-fold refusal: `normaliseTitle` folding to `''` (any non-Latin title — the `|samg` production bug) stores NULL and the row is excluded from key joins via partial indexes — honest refusal, not a degenerate key. Push protocol = full-snapshot PUT per source with per-source bearer token, DELETE+insert in one batch, 422 on empty array (the bridge script's zero-rows-is-a-missing-file rule kept), `pushed_at` surfaced in /health so staleness is visible — snapshot-replace is what makes the re-run-drift class structurally impossible. Sources push RAW strings, no fold code in any source; the index's fold pinned to library's normaliseTitle by a fixture file (`data/match-fold.fixtures.json`, the UNIVERSES mechanism again) + CI test in each repo. Read surface never auto-acts, so title-only game matching is safe there; fuzzy matching stays in the catalogs' matching.ts.

[2026-08-13T17:40-07:00] UNSOLVED / NEXT (session limit ~now; build deliberately not started — a half-scaffolded worker with unverifiable typecheck would be worse than none): build steps are §7 of the design doc, in order: (1) `catalog-platform/apps/index-worker/` — wrangler.toml, D1 `index_catalog`, the §4 migration, PUT /api/push/:source + GET lookup/universe/health, fold ported from library normaliseTitle WITH the fixture file created in the same commit; (2) `Board_Game_Catalog` `buildIndexProjection()` (allow-list from `item`: id, title, kind, parent_item_id→parent_source_id, year_published, publisher, cover/detail URLs — NEVER prices/condition) + cron push, ⚠️ no test script there, typecheck only, say so; (3) proof = 836 rows pushed, lookup answers. Open Qs in design §9 (push granularity — start cron-only; universe-on-write accepted; read-auth is an OWNER call before index.heygabi.ai goes live). Not verified: games item count 836 taken from the survey, not re-read tonight; §1's library "341 works" likewise from the survey.

[2026-08-13T18:05-07:00] CLAIM  Index Worker BUILD, design §7 steps 1–3 (step 4 explicitly OUT) — Fable 5, fresh session. (1) `catalog-platform/apps/index-worker/`: wrangler.toml (own blast radius), D1 `index_catalog` (authorised to create it; database_id will be reported), §4 migration, fold ported from library `normaliseTitle`/`primaryAuthor` WITH `data/match-fold.fixtures.json` in the SAME commit, universes lookup as a third fixtures-pinned implementation, PUT /api/push/:source + GET lookup/universe/health. (2) library gains the fold-fixtures CI test (sync-universes mechanism extended). (3) `Board_Game_Catalog` `buildIndexProjection()` (allow-list from `item`, all kinds, parent_item_id→parent_source_id) + push after mutations + daily-staleness backstop riding the PROVEN half-hourly cron (no new cron registration — the registration-flakiness lesson). Proof: 836 items pushed locally, lookup answers. NOT deploying the index Worker (read-auth is the owner's open §9 Q3); games repo has NO test script — typecheck only there, will say so.

[2026-08-13T18:55-07:00] DONE  Index Worker BUILD, design §7 steps 1–3 — commits: catalog-platform `1754542` (worker + fixtures) + `a318a4c` (docs unstale), library `aef4b25` (fold-pin CI test), Board_Game_Catalog `bb78448` (projection + push), all pushed. **D1 `index_catalog` created, database_id `3004d175-3c51-4ed4-ac3e-62859319f8ac`** (WNAM); ⚠️ its REMOTE migration is deliberately unapplied — `npm run db:migrate` in apps/index-worker is a pending owner command riding the gated deploy, per the no-unattended-migration rule. Worker NOT deployed (§9 Q3 read-auth stays the owner's). **The proof ran and answered**: 836 production items seeded into an isolated local games D1 (custom persist dir, deleted after; default local DBs untouched), pushed through the REAL cron path (`__scheduled` → backstop → PUT /api/push/game → 836 rows), then `lookup?title=taverns and dragons` returned *Taverns & Dragons* (catalog spells it `&`, query typed `and`), `The Dungeons & Dragons: Castle Ravenloft Board Game` matched with an ADDED leading article, and a promo answered carrying kind=promo + parent_source_id=104 — the first cross-catalog query surface this estate has answered without a script. Fold audit over the real data: 0/836 unfoldable, 0/836 work_fold (NULL for every game — the design held), 665 with parents, all 5 kinds. After-mutation middleware exercised live (PATCH → `index push (mutation) {"pushed":836}`, pushed_at advanced); backstop then SKIPPED as fresh (logged). Fixture pin CI-checked on BOTH sides in the same day: platform 17/17, library 375/375 (+4), library typecheck green; games typecheck green — ⚠️ games has NO test script, typecheck is the only verification there.

[2026-08-13T18:55-07:00] UNSOLVED / design self-critique, per rule 3 — what building it showed the design got wrong or half-said: (1) **§7 step 2 ("cron + after mutations") and §9 Q1 ("start cron-only") contradict each other**, and neither thought about WRITE VOLUME: a naive ride on the proven half-hourly cron would cost ~80k index D1 row-writes/day (48 × 836 × delete+insert). Built as: after-mutation push + a half-hourly backstop that GETs /api/health and pushes only when the game source is empty or >24h stale — the design's own registration-flakiness lesson made "its existing cron" the right reading, but the freshness gate is new and the doc should have specified it. (2) **The fixture contract needed a subtlety the design missed**: the file must record the RAW fold (`''` for Korean titles), because library CI asserts bare `normaliseTitle`; the NULL refusal is pinned separately, index-side only. Both suites also assert the empty-fold cases STAY in the file, so trimming fixtures can't silently unpin the load-bearing class. (3) **The `?unknown` sentinel was unhandled**: folding it yields `'unknown'`, colliding with a real "Author Unknown" credit — the index now refuses it before folding (creator_fold NULL). Matters at step 4, cheap to close now, absent from the design. (4) **Ownership does not travel, and the design never said what lookup therefore MEANS**: 29 of the 836 are wanted/sold-only, so the store answer is "in your catalog — tap through for owned-vs-wanted", not "you own it". Chose to push all catalogued things (§2's own words) with detail_url answering status one tap deeper; the owner should hear this before trusting lookup at a store. (5) Promoted the design's "creator NULL for games" schema comment to a 422 refusal on push. Not verified by execution: the remote push path end-to-end (needs the gated deploy) and D1 REMOTE batch limits with an 837-statement batch (local accepted it; remote could differ — first real push will tell). Local residue: games `.dev.vars` (gitignored) gained INDEX_URL/INDEX_PUSH_TOKEN dev entries pointing at :8788; the index worker's default local D1 retains the 836-row snapshot as dev data.

[2026-08-13T19:20-07:00] CLAIM  Estate-wide auth DESIGN (owner-approved scope: design doc only, no build/deploy/migration) — Fable 5. Writing `catalog-platform/docs/info/estate-auth-design.md`. Ground verified before writing, two brief-corrections found: (1) Board_Game_Catalog is ALREADY on Firebase ID tokens (`FIREBASE_PROJECT_ID = "audiobook-catalog"` in BOTH wrangler.tomls; Access deleted 2026-08-10 per heygabi-home README) — PLATFORM.md §1's "Auth: Cloudflare Access" row is stale; (2) library roles are owner|manager|reader|pending (0008), not owner|reader|pending. Also found: the two auth.ts files have ALREADY drifted — games hardened its dev bypass to `ENVIRONMENT === 'development'` with a warning that `!== 'production'` was dangerous, library still ships `!== 'production'`. That drift is the design's exhibit A. 12 FKs on library app_user confirmed by grep; audiobook /users = 3 frozen passphrase docs, Google users leave only localStorage + review displayNames + profiles/{slug}.

[2026-08-13T20:15-07:00] DONE  Estate-wide auth DESIGN — `catalog-platform/docs/info/estate-auth-design.md` (+ README row), commits `a38c88b`/`d40e1b8`/`a4d9b85`, pushed. NOTHING built, deployed or migrated. The spine: identity is ALREADY estate-global (both Workers pin Firebase project `audiobook-catalog` — the project is smaller than briefed); what is missing is MEMBERSHIP, so the design adds a three-value directory (`pending/approved/revoked`, an `is_approver` flag, NO roles) in a dedicated auth Worker + D1 at auth.heygabi.ai, consulted after local token verification via `POST /api/estate/seen` (per-app bearer), cached 10 min on each app's own app_user row — the TTL IS the revocation delay, named. Core semantic (§3.1): the estate gates newcomers and revocations, never overrules a standing local approval — that asymmetry is what keeps a seed gap or an auth outage from locking out the household while still failing closed for strangers. Search on the apex = index reads gated to members (closes index design §9 Q3 → the pending index deploy is unblocked once the owner approves this); the apex "NO AUTH EVER" comment is overturned WITH the reasoning on record (origin-scoped Firebase state; the one unverified load-bearing claim, flagged for an attended two-tab test). New-site inheritance = canonical `estate-auth` module in catalog-platform (the auth.ts drift is exhibit A — games hardened its dev bypass, library never heard) + 8 runnable conformance probes. Rollout §9: 8 steps, each reversible, INDEX ADOPTS FIRST (rejected the brief's library-first — prove the protocol where nobody can be locked out), then library shadow→enforce, then games; audiobook "user migration" = seeding from the two app_user tables + ADMIN_EMAILS (measured: the 4 review-name-only people have no email on record ANYWHERE and can only be met at the door; 3 passphrase docs stay frozen). Auth Worker has NO first-claim bootstrap on purpose — OWNER_EMAILS only. Owner must answer §13's 7 questions before build; headline three: does app-wide login gate audiobook READS (assumed no), confirm the default-grant (one approval → library reader + games viewer, which flattens a per-app privacy boundary), 10-min TTL ok.

[2026-08-13T21:05-07:00] CLAIM  Estate auth BUILD, design §14.1 + §14.2 ONLY (owner answered all seven §13 questions; audiobook site UNTOUCHED; index/apex/library/games adoption are later runs) — Fable 5. Scope: `catalog-platform/apps/auth-worker/` (own D1 `estate_auth` — authorised to create, id will be reported; §4.2 schema; §4.4 API with owner's amendment: NO admin page, owner-gated admin API with CORS locked to https://heygabi.ai + dev origin; §3.1 semantics exact; rate-limit ported; NOT deployed, remote migration NOT applied — both the dispatcher's) + `catalog-platform/packages/estate-auth/` (canonical verifier in the hardened `=== 'development'` form, §3.1 combine, /seen client with TTL named as the revocation delay = 10 min, `public:` posture declaration, §8.2 probes as executable checks) + `scripts/seed-estate.mjs` (dry-run default, accepts a later pre-seed list, never blocks on it). Exercising everything against LOCAL D1 via spawned wrangler dev, killed by name. Worker-first if budget bites.

[2026-08-13T22:20-07:00] DONE  Estate auth BUILD §14.1 + §14.2 — catalog-platform commits `f302ff8` (canonical module) + `951806a` (auth Worker + seed), pushed; tree clean. **D1 `estate_auth` created, database_id `d94ffe45-4dd0-4dc2-86de-b8c4d649c1cb`** (WNAM). ⚠️ NOT deployed, remote migration NOT applied, no route, no consumer wired — all deliberately the dispatcher's. **Module** (`packages/estate-auth/`): hardened verifier (`=== 'development'`, iss+aud pin, unverified-email refusal), §3.1 combine pinned row-by-row (8 rows as 8 tests), /seen client with `REVOCATION_DELAY_MS` = 10 min named, `public:` posture declaration (public+defaultRole is a throw — an open door grants nothing), §8.2 probes as executable checks that always report all eight and PROVE they catch failing-open sites (tested against a deliberately-holed mock server). **Worker** (`apps/auth-worker/`): §4.2 schema verbatim; /seen upsert is ONE statement that structurally cannot change status, per-app timing-safe bearers, OWNER_EMAILS answered approved COMPUTED never stored; admin API per owner answer #6 (NO admin page, CORS locked to https://heygabi.ai; promotion via POST :id/approver, 409 unless approved first); rate-limit ported. **Verified by execution**: 29/29 live probes against real wrangler dev + LOCAL D1 in 4 phases — owner-on-EMPTY-table works with no first-claim rule (§4.3), stranger/non-approver 403 + no row created, revocation survives re-sign-in, decided_at/decided_by stamped with the actor's row id, conformance #2/#3 pass in real-auth mode, CORS refuses non-apex origins; plus 51 unit tests (31 module + 17 index + 3 worker), typecheck all workspaces, port 8799 swept + 0 workerd after. **Seed** (`scripts/seed-estate.mjs`): dry-run default printing every row, --commit demands explicit --local|--remote, INSERT OR IGNORE + is_approver-upgrade-only, refuses zero-row reads, --extra accepts the later pre-seed list without blocking (owner answer #5), --sources-local rehearsal lane refuses --remote; exercised locally end-to-end (dry-run, two commits, idempotent single row).

[2026-08-13T22:20-07:00] UNSOLVED / design errata + residue, per rule: (1) ⚠️ **The seed's REMOTE dry-run was NOT executed** — the permission classifier blocked `wrangler d1 execute --remote` reads of the two production app_user tables from this agent run; the read path is built and exercised via --sources-local, but the dispatcher must run `node scripts/seed-estate.mjs` (dry-run, eyeball every row) before any `--commit --remote`. (2) Not verified by execution: a REAL Firebase token through the JWKS path (every live probe used dev bypass or garbage tokens — a genuine token needs a signed-in human; first owner sign-in after deploy is the probe), the rate limiter with a real binding (fails open without, by design), CORS in an actual browser. (3) Design errata found by building: §14.1's "§8.2's eight as local-D1 probes" overpromises — three of eight need human-held credentials (fresh Google sign-in, other-project token, console facts), so the probes always report all eight with visible skips instead; local D1's `--json` meta has NO `changes` field, so seed idempotency cannot be counted, only read back (script says so rather than inventing a number); Windows shell:true concatenation shreds `--command` SQL — everything goes via `--file`; /seen with a firebase_uid already on another row (account email change) would have 500'd — handled with a uid-less retry, worth a design §4.4 footnote. (4) Residue: `apps/auth-worker/.wrangler/` holds probe + seed-rehearsal local state (gitignored dev data); `.dev.vars.example` documents dev vars, real `.dev.vars` never created. **NEXT RUN resumable position**: §14.3 index adoption (add `estate_cache` to the index Worker's still-unapplied 0001, wire the module onto GET lookup/universe, mint ESTATE_APP_TOKEN_INDEX) → §14.4 apex search + the admin PAGE on the apex (calls this Worker's admin API; CORS is already locked to it) → §14.5 library shadow→enforce, then games. Dispatcher-owner steps before any consumer works: deploy auth Worker + auth.heygabi.ai route, `npm run db:migrate` (remote), `wrangler secret put` ×3, seed dry-run then commit. NO Firebase authorised domain needed for auth.heygabi.ai (admin UI is on the apex — owner answer #6).

[2026-08-13T23:05-07:00] CLAIM  Estate auth §14.3 — INDEX WORKER ADOPTS (first consumer; zero users, per §9 step 3) — Fable 5. Ground re-verified: index 0001 IS applied remotely (`wrangler d1 migrations list --remote` → no pending, re-checked tonight), so §14.3's "fold estate_cache into the unapplied 0001" is STALE — `estate_cache` becomes an ADDITIVE `0002`. Scope: (1) `apps/index-worker/migrations/0002_estate_cache.sql` (email PK / uid / status / checked_at — the index has no app_user, this plays its cache role); (2) canonical `@platform/estate-auth` wired onto GET lookup + universe ONLY — verify locally → cached /seen → §3.1 combine with local = OWNER_EMAILS-membership (no local roles: membership IS the authorization, default_grant grants nothing and proceeds); health stays open; PUT /api/push/:source keeps its per-source bearers UNTOUCHED, mounted before the blanket by name (§8.2 #3); (3) wrangler.toml gains [vars] ENVIRONMENT=production + FIREBASE_PROJECT_ID + OWNER_EMAILS + ESTATE_AUTH_URL, `.dev.vars.example` documents ESTATE_APP_TOKEN_INDEX; (4) §8.2 conformance probes run against real `wrangler dev` + LOCAL D1 (credential-bound probes visibly skip, per the module's design) plus an end-to-end phase with a spawned local auth Worker (approved→results, fresh→pending, revoked→403, unreachable→standing served / unknown refused / owner break-glass). NOT deploying, NOT applying remote migrations, NOT touching secrets — all the dispatcher's. Ports 8788+8799 swept, workerd 0 before finishing (baseline 0 verified).

[2026-08-14T00:20-07:00] DONE  Estate auth §14.3 — INDEX ADOPTS, first consumer — catalog-platform commits `3f004ca` (auth-worker break-glass fix) + `1576c17` (index adoption) + `3a0a555` (docs unstale), pushed; tree clean; workerd 0, ports 8788/8799 clear. **Built**: additive `0002_estate_cache.sql` (email PK/uid/status CHECK/checked_at — 0001 confirmed applied remotely, the §14.3 fold-in note was stale as briefed); `requireEstateMember()` blanket over GET lookup+universe (canonical module only — verify local → cached /seen → §3.1 combine; local standing = OWNER_EMAILS alone; `default_grant` grants nothing and proceeds, membership IS the authorization); health split to its own open route; push per-source bearers UNTOUCHED, mounted before the blanket by name; wrangler.toml [vars] ENVIRONMENT=production explicit (§8.2 #8) + posture `public:false/defaultRole:null` declared. **Verified by execution**: 16 new wiring tests against the real exported app (33 index total, 67 workspace-wide, typecheck green) + 13/13 live probes — §8.2 conformance in real-auth mode (#2 #3 PASS by execution: garbage token 401, 2 protected tokenless 401, health 200, push machine route refused; #1 #4–#8 visibly skipped, credential/console-bound per the module's design), then the full lifecycle against a REAL spawned local auth Worker: fresh sign-in queued `seen:index` (per-app bearer wiring proven), approve/revoke both land only after the cache ages (TTL real in both directions; aged via `d1 execute --file`, WAL-safe beside the running dev server), stale-approved cache rides an auth outage, unknown+outage → 503 `estate_unreachable`, OWNER_EMAILS break-glass with the directory DOWN. ⚠️ **Genuine auth-Worker defect found and fixed** (the announce-loudly clause): `materializeOwnerRow` 500'd on a firebase_uid UNIQUE collision — the account-email-change class `seenUpsert` already absorbed was never given to the BREAK-GLASS path, the worst place to 500; probes tripped it instantly (all dev-bypass identities share 'dev-uid'). Fix mirrors seenUpsert's uid-less retry; auth-worker 3/3 + full probe suite green after. **Dispatcher deploy step needs**: on the INDEX worker — `wrangler secret put ESTATE_APP_TOKEN_INDEX` (value = what the AUTH worker already holds under the same name), push-token secrets ×3, `npm run db:migrate` (remote 0002), route + deploy; ESTATE_AUTH_URL/OWNER_EMAILS/FIREBASE_PROJECT_ID already in [vars]. Not verified by execution: a real Firebase token through JWKS (needs a human sign-in), the remote 0002 apply, production /seen latency. Residue: index `.wrangler/probe-state` + auth-worker `.wrangler/index-probe-state` hold probe dev data (gitignored).

[2026-08-13T20:10-07:00] CLAIM  Estate auth §14.4 — apex global search + owner admin page — Fable 5. Scope: `sites/heygabi-home/public` gains Firebase sign-in (ported minimum from audiobook `identity.js`; popup-first, redirect fallback, `auth/unauthorized-domain` rendered as an owner-action message), search in the reserved `#find` slot querying `index.heygabi.ai/api/lookup` with the ID token (results grouped books-tier/games, ownership caveat worded per index-design — "in your catalog, tap through", never "owned"), and `/admin` calling the auth Worker's admin API (CORS already locked to the apex): list by status, approve/revoke, promote-to-approver, break-glass surfaced. The `:13` NO-AUTH warning REWRITTEN per design §7.2 (not deleted); `_headers` CSP widened per-path (apex + /admin widened, /todo keeps `default-src 'none'` no-JS). NOT deploying — apex deploy is the dispatcher's (`npx wrangler pages deploy sites/heygabi-home/public --project-name heygabi-home`). §15 two-tab test becomes DUE the moment this deploys. No Worker/module changes expected.

[2026-08-13T20:35-07:00] DONE  Estate auth §14.4 — apex search + /admin — catalog-platform commits `c41e1b2` (build) + `e5e5945` (design doc unstale), pushed; tree clean; NOT deployed (apex deploy is the dispatcher's: `npx wrangler pages deploy sites/heygabi-home/public --project-name heygabi-home`, run from repo root — the apex is Pages project `heygabi-home`, direct upload, verified against deploy.md/README not assumed). **Built**: `#find` search (signed-out: page whole, box asks for sign-in; results in the index design's two tiers — books = library+audiobook same-work-any-format, games = title-only with kind/parent_source_id — plus "everything in <universe>" follow-up; ⚠️ caveat line is the index design's own meaning, "in the catalog — tap through for owned-vs-wanted", NEVER "you own this"); `/admin` member directory against auth.heygabi.ai's admin API (CORS already locked to the apex — owner decision #6): pending-first list, approve/revoke, promote-to-approver (409 not_approved surfaced in the API's own words), OWNER_EMAILS break-glass stated on the page face; `assets/estate-auth.js` = ported MINIMUM of identity.js (popup-first, POPUP_UNAVAILABLE-only redirect fallback, handleRedirectResult on every load) but KEEPING the session — its job is minting bearers; `auth/unauthorized-domain` detected and rendered as a named owner-action message. The `:13` NO-AUTH warning REWRITTEN per §7.2 with the arguing kept; README/deploy.md's inverse rules fixed (stale "never authorise the apex" rows were now-dangerous). `_headers` CSP per-path: / and /admin allow-listed (gstatic SDK, apis.google.com, identitytoolkit/securetoken, index.+auth.heygabi.ai, cover/avatar img hosts; inline script still blocked), /todo keeps `default-src 'none'` — NOT on /* because two matching CSP rules enforce their intersection. **Verified**: node --check on all 3 modules, workspace typecheck green ×3, 67/67 tests, workerd count 0 (no dev server was started). 🔴 **Owner steps at deploy**: add `heygabi.ai` to Firebase authorised domains (console-only — YES it is needed, sign-in cannot work without it), then deploy, then ⚠️ **the §15 two-tab test is DUE** (owner-attended): sign in on the apex → second tab audiobooks.heygabi.ai (it signs its own auth instance out on load) → PASS = apex tab still signed in AND the audiobook site's own sign-in/sign-out unaffected; FAIL = §7.2's fallback triggers (search.heygabi.ai), deliberately NOT pre-built.

[2026-08-13T20:35-07:00] UNSOLVED  §14.4 residue, named per rule: (1) **No browser drove any of it** — the modules are verified by syntax check + contract-reading of the live Workers' code (error vocabularies matched to middleware/auth.ts and estate.ts verbatim); the untested seams are the Firebase popup on a real apex origin, CORS in a real browser, and CSP fit (does the popup flow need apis.google.com/accounts.google.com frames? — allowed speculatively, remove if observed unused; _headers cannot be exercised locally at all). Settled by: the owner's first signed-in visit after deploy with the console open. (2) The CSP img-src host list is a best-effort enumeration (covers/bookcovers.heygabi.ai, openlibrary, books.google, cf.geekdo-images, lh3.googleusercontent) — a cover on any other host renders blank (find.js hides broken images); add hosts as observed, deliberately not `https:`. (3) www.heygabi.ai serves the page but its /admin CANNOT work (Worker CORS names the apex exactly) — admin.js detects and says so; the §2.1 www→apex redirect remains outstanding and would retire the case. (4) Universe follow-up buttons re-query /api/universe/:name — fine at household scale, no debounce/cache; noted in case someone taps like a woodpecker. (5) Books-tier grouping renders today as an empty group (index holds 836 game rows only) until the library/audiobook pushers exist (§14.5+/design §7 step 4).

[2026-08-13T20:22-07:00] CLAIM  Estate auth §14.5 — LIBRARY ADOPTS IN SHADOW (observe and log, enforce nothing) — Fable 5. Scope: (1) additive migration `0140` — `app_user` gains the two §5.2 cache columns (`estate_status`, `estate_checked_at`), local D1 exercised, NOT applied remote; (2) canonical `@platform/estate-auth` reaches this repo the universes way — new `scripts/sync-estate-auth.mjs` materialises its src into gitignored `packages/estate-auth/generated/`, thin `@lc/estate-auth` workspace package wraps it plus the library's shadow orchestration; (3) `middleware/auth.ts` gains the shadow step AFTER local auth resolves: cached `/seen` → §3.1 combine (`active = role !== 'pending'`, `locallyDecided = approved_at IS NOT NULL`) → one greppable `estate_shadow` JSON log line per request, response NEVER changed; default-grant posture declared (`reader`) and visible in the logs as would-auto-grant, nothing written; (4) `ESTATE_CHECK` env flag `off|shadow|enforce`, default/deployed `off` so the deploy is inert; absent URL/secret = named log + behave as off; `enforce` deliberately NOT built — logs loudly and behaves as shadow; (5) tests for the shadow path added to the root suite, 375 existing must stay green; (6) live probe via wrangler dev + a stub auth server, dev servers killed by name. NOT deploying, NOT applying remote migrations, NOT touching secrets, NOT editing catalog-platform (games agent runs parallel — FABLE5 pull-rebase before every push, keep both hunks on conflict).

[2026-08-13T20:23-07:00] CLAIM  Estate auth §14.5 — GAMES adopts in SHADOW MODE (parallel to the library adoption; both log here — on a FABLE5 conflict keep BOTH hunks) — Fable 5. Scope, Board_Game_Catalog only: additive migration 0026 (app_user estate_status/estate_checked_at, local apply only); canonical `estate-auth` module materialised via the library's sync-universes mechanism (new build-dependency on catalog-platform — README will say so); verifier swap per §8.1 (behaviour-identical: the canonical hardened `=== 'development'` bypass CAME from this repo); `/seen` cached on app_user + §3.1 would-verdict logged with a greppable WOULD-DENY token, response untouched; `ESTATE_CHECK` off|shadow|enforce, default off in [vars] so the deploy is inert; default-grant config `viewer` present but inert in shadow. OWNER_EMAILS recovery hatch and rate limiter undisturbed. ⚠️ Games has NO test script — typecheck + wrangler-dev probes against local D1 (mock /seen server in scratchpad, NOT a .dev.vars in catalog-platform — that tree is another agent's). NOT deploying, NOT applying the migration remotely, NOT touching catalog-platform. wrangler killed by name, workerd 0 before finishing.

[2026-08-13T20:36-07:00] DONE  Estate auth §14.5 — GAMES shadow adoption — Board_Game_Catalog commits `0077a7a` (build) + `03e8827` (HANDOFF), pushed; catalog-platform untouched; workerd 0, probe rows deleted. **Built**: canonical module materialised via a ported sync-universes mechanism (`scripts/sync-estate-auth.mjs` as predev/pretypecheck/predeploy, gitignored copy at `apps/worker/src/estate-auth/`, loud failure without the sibling checkout — README says so per §14.2); verifier swap per §8.1 (behaviour-identical, canonical hardened bypass came FROM this repo; the swap now also sends firebase_uid to /seen, which the old local Identity couldn't); `middleware/estate.ts` runs §5.2 after `upsertUserOnLogin` (OWNER_EMAILS hatch and rate limiter untouched) at ESTATE_CHECK strength — `off` default in [vars] (deploy inert; unrecognised values fall to off loudly), `shadow` logs the §3.1 verdict with a greppable **WOULD-DENY** token and changes no response, `enforce` fully built (revoked→403 computed-not-stored, unreachable-no-standing→named 503, default-grant writes `viewer` with approved_by NULL as the estate-actor convention — no change_log in this repo, the log line is the audit); migration `0026` additive ADD COLUMN, LOCAL only. **Verified by execution** (⚠️ no test script in this repo — typecheck is the only static verification, plus 11 wrangler-dev probes against local D1 with a scratchpad mock /seen, NOT a .dev.vars in catalog-platform): off inert (0 mock hits, no log); shadow → /seen carries `Bearer` app token + uid, TTL cache honoured (3 requests, 1 call), revoked logs WOULD-DENY while answering 200 even for a local owner, stale-approved cache rides an outage, estate-approved local-pending logs would-grant and role STAYS pending, config-unset warns per request and skips; enforce → default-grant lands viewer (capabilities [read] only — the 0023/0024 rater distinction held), revoked 403, fresh-user-during-outage 503 estate_unreachable, standing owner served through the outage on the stale cache. **Dispatcher deploy step needs**: `npm run db:migrate` (remote 0026, BEFORE the deploy), `npm run secret ESTATE_APP_TOKEN_GAMES` (auth Worker already holds the value), deploy, then flip ESTATE_CHECK to "shadow" in wrangler.toml + redeploy when ready; read shadow via `npm run tail --workspace @bgc/worker`, grep WOULD-DENY, expect zero for the two approved household members.

[2026-08-13T20:36-07:00] UNSOLVED / residue, named per rule: (1) Not verified by execution: a REAL Firebase token through JWKS (all probes rode the dev bypass — the canonical verifier's JWKS path is unchanged code, but this repo never exercised it locally), the remote 0026 apply, production /seen latency, and the real auth Worker end-to-end (a mock answered /seen; the §3.1 combine itself is pinned by catalog-platform's own module tests). (2) `enforce` is built and probed locally but deliberately unexercised against production — §14.5 says days of clean shadow first. (3) The estate cache read/write is a second small D1 query per request rather than widening `upsertUserOnLogin`'s SELECT — deliberate (AppUser type and web app stay ignorant of the cache), fold into the user row read later if enforce makes it annoying; noted in packages/db/src/estate.ts. (4) Games' misconfigured-FIREBASE_PROJECT_ID error lost its `docs/SETUP.md step 7` pointer in the verifier swap (canonical message is terser) — cosmetic, lives upstream now. (5) No module defects found to report to catalog-platform this run.

[2026-08-13T20:41-07:00] DONE  Estate auth §14.5 — LIBRARY SHADOW ADOPTION — library_catalog commit `e5a9c3d`, pushed; tree clean; 393/393 tests (375 pre-existing untouched), typecheck green all workspaces; library node/workerd count 0 after a kill-by-name sweep. **Built**: additive 0140 (`app_user.estate_status` CHECKed + `estate_checked_at` — exercised on local D1: values land, `'banana'` refused; NOT applied remote); canonical module via a SECOND sibling-checkout sync (`scripts/sync-estate-auth.mjs` → gitignored `packages/estate-auth/generated/`, `@lc/estate-auth` wraps it; refuses unexpected/missing upstream files by name); shadow step in `middleware/auth.ts` AFTER local auth, whole block try/caught so no estate failure can touch a response; posture `public:false / defaultRole:'reader'` declared, surfaced only as `would_auto_grant` in logs; `ESTATE_CHECK` off|shadow|enforce with off = deployed default (zero DB read/fetch/log), unrecognised → off + `mode_unrecognised` line, **enforce NOT built** (loud `enforce_requested` marker, behaves as shadow); absent URL/token under shadow → `estate_config_unset` naming the missing var(s), no fetch — code-before-secret deploys are safe. **Verified by execution**: 18 unit tests (mode semantics, §3.1 would-verdicts incl. locally-demoted-pending and seed-gap local-wins, TTL both directions, garbage cache, log-line shape) + six live phases against wrangler dev + a stub directory: off inert (0 log lines), config-unset named, fresh `/seen` then cache (stub saw exactly 1 call; columns persisted, read back), revoked → `would_deny:true` with the response still 200, stub killed + aged cache → `stale_cache` ride, enforce → marker. **Dispatcher deploy step needs**: `npm run db:migrate` (remote 0140), `wrangler secret put ESTATE_APP_TOKEN_LIBRARY` (value = the auth Worker's same-named secret; or `.dev.vars` + `secrets:push` — allowlisted), deploy (ESTATE_CHECK="off" ships inert), then flip `ESTATE_CHECK` to `"shadow"` in wrangler.toml + redeploy; read via `npx wrangler tail --config apps/worker/wrangler.toml --format json | grep estate_shadow`, gate on zero `"would_deny":true` household lines over days (both household users are already approved in the directory, so ANY would-deny is a finding). Docs: `docs/info/estate-auth-shadow.md` + README row + CLAUDE.md row.

[2026-08-13T20:41-07:00] UNSOLVED  §14.5 library residue, named per rule: (1) **No real Firebase token crossed the JWKS path** — every live probe used the dev bypass; the local-verifier half is unchanged code, but shadow's first production reading is the owner's first signed-in visit after the dispatcher flips `shadow`. (2) **The real auth Worker was never in the loop** — the stub speaks the /seen contract as the canonical module's client defines it (status echoed from a control file, bearer asserted); contract drift between module and deployed Worker would surface as `src:"none"`/unreachable lines in production shadow, which is exactly what shadow is for. (3) `wrangler dev` does NOT hot-reload `.dev.vars` here (measured — the watcher never fired; every mode flip needed a restart); recorded in the info doc. (4) Windows localhost connect-refused burned ~2s before the stale-cache fallback (seen_ms 2025–2034 during outage phases) — production behaviour against a real down Worker unmeasured; once per user per TTL, not per request. (5) ⚠️ Process hygiene incident, owned: an inherited leaked dev server held 8787 and my phase-A curls read the WRONG worker until caught; killing it by PID also killed what turned out to be the parallel games agent's workerd (its wrangler respawned it within seconds, verified listening again). Lesson applied afterwards: kill filtered on `CommandLine -match 'library_catalog'` only. (6) The canonical module itself: NO defect found — consumed as-is, zero stubs, zero local patches.

## 8. Budget rules

From the global usage rules, and they are not optional:

- Pause the **session** window at **89%**
- On the **weekly** limit: **stop starting new subagents at 93%**, keep working
  conversationally only to **97%**
- ⚠️ **A subagent's cost is invisible until it lands** — one landed at 372k tokens
  in a single lump. **The granularity of risk is one agent, not one tool call.**
  Pulse-check usage whenever an agent is in flight, and state the figure when
  reporting so a stale or failed read is visible rather than assumed fine.
- Fable's own allowance is separate; the rules above still apply to the
  all-models pool that the dispatching session spends.

### ⚠️ 6.1 Fable does NOT watch its own usage — the dispatcher does

Asked directly by the owner, 2026-08-13, and the answer is no. Two reasons, and
they are the reason the rule is written the way it is:

1. **The cost is invisible until the agent lands** — that is the entire failure
   mode, and it is invisible *to the dispatcher*. An agent that reads the usage
   page mid-run reports a figure that does not yet include its own consumption, so
   a self-check is structurally incapable of catching the thing being guarded
   against.
2. The global rules say outright to **use a background timer rather than a
   dedicated monitor agent**, because an agent costs more context than the tool
   calls it would replace. A self-monitoring agent is that anti-pattern twice.

**So: the dispatching session pulse-checks between dispatches and states the
figure when reporting.** ⚠️ And note that Fable being on a separate allowance makes
its work *cheap on the main pool, not free* — the dispatcher still pays for the
prompts it sends and the reports it reads.
